@props(['width' => 400])

<img src="/assets/images/logo_aiter_sem_fundo.png" alt="Logo Aiter (claro)" width="{{ $width }}"
    class="hidden dark:block" />
<img src="/assets/images/logo_aiter_sem_fundo_black.png" alt="Logo Aiter (escuro)" width="{{ $width }}"
    class="block dark:hidden" />
